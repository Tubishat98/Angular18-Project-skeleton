/**
 * User role enumeration
 * Defines all possible user roles in the application
 */
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  MANAGER = 'manager',
  GUEST = 'guest',
  MODERATOR = 'moderator'
}

