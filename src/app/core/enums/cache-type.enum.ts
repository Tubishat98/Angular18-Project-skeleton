/**
 * Cache type enumeration
 * Defines different cache storage types
 */
export enum CacheType {
  MEMORY = 'memory',
  LOCAL_STORAGE = 'localStorage',
  SESSION_STORAGE = 'sessionStorage',
  INDEXED_DB = 'indexedDB'
}

