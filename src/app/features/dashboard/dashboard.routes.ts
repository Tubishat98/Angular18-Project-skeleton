import { Routes } from '@angular/router';

/**
 * Dashboard routes
 * Note: This file is kept for backward compatibility but routes are now handled in app.routes.ts
 */
export const dashboardRoutes: Routes = [];

